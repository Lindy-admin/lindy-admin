require "rails_helper"
require "mollie_helper"

describe "When updating a payment status through the webhook" do

  context "with a payment that changed to payed" do

    pending "changes the payment status to payed"
    pending "returns the payment"

  end

  context "with a payment that changed to error" do

    pending "changes the payment status to error"
    pending "returns the payment"

  end

  context "with a payment id that doesn't exist" do

    pending "returns a NOT FOUND status"

  end

end
